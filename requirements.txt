# Core runtime dependencies for nmap-ai.
# Only includes packages actually imported by the codebase.
# Dev/test/docs tools live in requirements-dev.txt.

# Scanning
python-nmap>=0.7.1

# CLI
click>=8.1.0
rich>=12.0.0

# Config / I/O
PyYAML>=6.0
requests>=2.28.0

# Heuristic analysis (used by ai/vulnerability_detector.py)
numpy>=1.21.0

# Web interface (FastAPI server in nmap_ai/web)
fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=1.10.0
jinja2>=3.1.0  # required by fastapi.templating used in web/main.py

# GUI (PyQt6) is intentionally NOT pinned here; gui_main.py prompts to install
# it on demand. Install separately with: pip install PyQt6>=6.4.0
